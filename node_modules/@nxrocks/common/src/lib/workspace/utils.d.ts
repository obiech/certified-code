export declare function getProjectRoot(project: {
    root: string;
}): string;
export declare function getProjectFilePath(project: {
    root: string;
}, relativeFile: string): string;
export declare function getProjectFileContent(project: {
    root: string;
}, relativeFile: string): string;
export declare function getNameAndRoot(file: string): {
    root: string;
    name: string;
};
export declare function getCurrentAndParentFolder(cwd: string): {
    currentFolder: string;
    parentFolder: string;
};
export declare function getProjectRootFromFile(filePath: string): string;
